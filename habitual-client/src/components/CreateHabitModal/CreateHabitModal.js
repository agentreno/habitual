import React from 'react'
import Modal from 'react-modal'

const CreateHabitModal = ({ show }) => (
  <Modal
    isOpen={show}
  >
    <h2>Loading...</h2>
  </Modal>
)

export default CreateHabitModal
